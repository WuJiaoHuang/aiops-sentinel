# Frontend Rules

- Use React with TypeScript for all UI components.
- Keep operation panels dense, readable, and task-focused.
- Provide loading, empty, and error states for incident diagnosis workflows.
- Avoid decorative UI that hides operational data.
- Keep data models imported from `@aiops-sentinel/core` when possible.
